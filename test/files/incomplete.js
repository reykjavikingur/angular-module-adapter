angular.module("myApp", [])
	.run(function(6 + abq) {
			console log("starting")[
			})
		.service("appService", function($resource) {
			this.load = fgunction() {
				$resource.load()
			}
		})